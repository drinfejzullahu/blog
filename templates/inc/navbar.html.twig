<nav class="navbar navbar-expand-sm navbar-dark bg-primary mb-3">
    <div class="container-fluid">
        <a href="/" class="navbar-brand">Crud</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#mobile-nav">
            <span class="navbar-toggle-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="mobile-nav">
            <ul class="navbar-nav ml-auto">

                {% if app.user %}
                    {% if app.user.admin == true %}

                        <li class="nav-item">
                            Hello <a href="/user/{{ app.user.id }}"
                                     class="navbar-text mr-3 ml-3  font-weight-light">   {{ app.user.username }} </a>
                        </li>
                        <li class="nav-item">
                            <a class=" btn btn-primary mb-1" href="/">Home</a>
                        </li>
                        <li class="nav-item">
                            <a class=" btn btn-primary mb-1" href="{{ path('user_index') }}">Users</a>
                        </li>
                        <li class="nav-item">
                            <a class=" btn btn-primary mb-1" href="{{ path('blog_new') }}">New Blog</a>
                        </li>
                        <li class="nav-item">
                            <a class=" btn btn-primary mb-1" href="{{ path('blog_index') }}">Posts</a>
                        </li>
                        <li class="nav-item">
                            <a class=" btn btn-primary mb-1" href="{{ path('app_logout') }}">Logout</a>
                        </li>
                    {% else %}
                        <li class="nav-item">
                            Hello <a href="/user/{{ app.user.id }}"
                                     class="navbar-text mr-3 ml-3  font-weight-light">   {{ app.user.username }} </a>
                        </li>
                        <li class="nav-item">
                            <a class=" btn btn-primary mb-1" href="/">Home</a>
                        </li>
                        <li class="nav-item">
                            <a class=" btn btn-primary mb-1" href="/contactus">Contact Us</a>
                        </li>
                        <li class="nav-item">
                            <a class=" btn btn-primary mb-1" href="{{ path('blog_new') }}">New Blog</a>
                        </li>
                        <li class="nav-item">
                            <a class=" btn btn-primary mb-1" href="{{ path('blog_index') }}">Posts</a>
                        </li>
                        <li class="nav-item">
                            <a class=" btn btn-primary mb-1" href="{{ path('app_logout') }}">Logout</a>
                        </li>
                    {% endif %}

                {% else %}
                    <li class="nav-item">
                        <a class="btn btn-primary" href="/user/new">Sign Up</a>
                    </li>

                    <li class="nav-item">
                        <a class="btn btn-primary" href="/login">Log In </a>
                    </li>
                {% endif %}


            </ul>
        </div>
    </div>
</nav>